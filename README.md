# stylelint-config-properties-order

## Installation

```
npm install --save-dev @paulsolari/stylelint-config-properties-order
```

## Usage

Add config to .stylelintrc

```
{
  "extends": [
    "@paulsolari/stylelint-config-properties-order"
  ]
}
```
